server:
  port: 8010
  max-http-header-size: 1048576
  tomcat: 
    max-http-post-size: 1048576
spring:
  application:
    name: ruigou-common-test
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true